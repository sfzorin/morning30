# Optional credentials for the Leap Fitness How-To import pipeline (scripts/).
# Both are optional: without YOUTUBE_API_KEY the fetcher falls back to yt-dlp;
# without OPENAI_API_KEY the card generator falls back to local house-style drafts.
YOUTUBE_API_KEY=
OPENAI_API_KEY=
